@Library('janek-pipeline-library') _

pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo "Hello world!"'
                log.info("Janek")
            }
        }
    }
}
